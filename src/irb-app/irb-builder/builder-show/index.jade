.section.builder-show
  if !report.sections.show.clauses.length
    .clause.comparison
      h3
      .control-container= $component.child('show-add-control')
  else
    for clause, clauseIndex in report.sections.show.clauses
      if clauseIndex
        .preposition and
      .clause.comparison
        .header
          h3= 'Show' + (report.sections.show.clauses.length === 1 ? '' : ' ' + (clauseIndex + 1))
        .control-container= $component.child('show-edit-control', {attributes: {'clause-index': clauseIndex}})
        if clauseIndex === report.sections.show.clauses.length - 1
          .control-container= $component.child('show-add-control')
