.section.builder-time
  if sections.time.clauses.length
    .preposition in the
    .clause.time
      h3 Time
      .control-container= $component.child('time-edit-control', {attributes: {'clause-index': 0}})
