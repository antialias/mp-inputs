include ./tooltip.jade

.irb-learn
  if learnActive
    - const step = util.getLearnStep(report, learnModalStepIndex);
    - const isModalStep = [`introduction`, `getting-started`, `conclusion`].includes(step.name);

    if isModalStep && !learnTransitioningOut
      mp-modal(attrs={open: true})
        .mp-modal-content
          .mp-modal-title
            case step.name
              when `introduction`
                | Introducing Insights
              when `conclusion`
                | Congratulations, your first Insights query is done!
          .mp-modal-subtitle
            case step.name
              when `introduction`
                | Built to make exploring your data easier than ever,
                br
                | Insights allows you to create complex queries that help
                br
                | you understand how your customers use your product.
              when `conclusion`
                | You’ve only scratched the surface on the power of Insights.
                br
                | Learn more about all of the additional ways you can analyze
                br
                | and visualize your data using Insights
                =" "
                a(href='docs-url-here') here
                | .
          .mp-modal-close(on={click: $component.app.helpers.finishLearn})
            svg-icon(attrs={icon: 'x'})
        mp-button-bar.mp-button-bar-primary(
          on={click: () => $helpers.clickedModalButton(step.name)}
        )
          case step.name
            when `introduction`
              | Let's get started
            when `conclusion`
              | Continue exploring your data
