include ./tooltip.jade

.irb-learn
  if learnActive
    - const step = util.getLearnStep(report, learnModalStepIndex);
    - const isModalStep = [`introduction`, `getting-started`, `conclusion`].includes(step.name);

    if isModalStep && !learnTransitioningOut
      mp-modal(attrs={open: true})
        .mp-modal-content
          .mp-modal-title
            case step.name
              when `introduction`
                | Introducing Inquiry
              when `conclusion`
                | You've built your first Inquiry query
          .mp-modal-subtitle
            case step.name
              when `introduction`
                | Insights makes exploring your data easier than ever. It allows you to formulate complex queries intuitively to help you understand how your customers use your product.
              when `conclusion`
                | You can save it, export it, add it to your dashboard, and even filter your results. Click the button below to start exploring Inquiry's powers on your own.
          .mp-modal-close(on={click: $component.app.helpers.finishLearn})
            svg-icon(attrs={icon: 'x'})
        mp-button-bar.mp-button-bar-primary(
          on={click: () => $helpers.clickedModalButton(step.name)}
        )
          case step.name
            when `introduction`
              | Let's get started
            when `conclusion`
              | Explore Inquiry on your own
