include ../mixins.jade

.builder-screen.sources
  ul.screen-options-list.irb-learn-disabled-except-first-child(
    hook={postpatch: $helpers.updateRenderedSize}
    on={scroll: $helpers.scrolledList}
  )
    if contextFilter
      +filtered-options-lists($helpers.getFilteredLists())
    else
      each source in $helpers.getSources()
        +generic-options-list-item(source.name, {
          onclick: () => $helpers.clickedSource(source),
        })
