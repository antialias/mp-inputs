.resize-input
  input(
    attrs={
      placeholder: $component.placeholder,
    }
    props={
      maxLength: 50,
      type: `text`,
      value: inputValue,
    }
    style={
      width: `${inputWidth}px`,
    }
    on={
      input: $helpers.updatedInput,
    }
    hook={
      insert: $helpers.inserted,
    }
  )
