.toast(style={display: showToast ? 'block' : 'none'})
  .toast-section.toast-message= $helpers.getMessage()
  button.toast-section.toast-action(on={click: $helpers.eatToast})= $helpers.getSelectText()
  button.toast-section.toast-exit(on={click: $helpers.trashToast})
