- let selectedTypeIndex = state.editing && constants.resourceTypeChoices.indexOf(state.editing.type);
- let selectedValueIndex = state.editing && state.topProperties.indexOf(state.editing.value);

div(class=elementClass)
  ul.column.resourceType
    for type, i in constants.resourceTypeChoices
      li(
        class=selectedTypeIndex === i ? 'selected' : '',
        ev-click=() => helpers.updateSection(state.clauseIndex, {type}),
      )= util.capitalize(type)
  ul.column.resourceValue
    for value, i in state.topProperties
      if helpers.matchesSearch(util.renameProperty(value))
        li(
          class=selectedValueIndex === i ? 'selected' : '',
          ev-click=() => helpers.updateSection(state.clauseIndex, {value}),
        )= util.renameProperty(value)
